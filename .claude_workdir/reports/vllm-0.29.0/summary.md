# vLLM sync summary — v0.28.0 → v0.29.0

Report-only run — no branch, no commits.
No earlier run to compare against.

| Status | Count |
|---|---|
| needs decision | 87 |
| auto-apply-eligible | 35 |

## Commits
_none_

## Auto-apply eligible (35)
- F-01 `baidu/ERNIE-4.5-VL-28B-A3B-PT.yaml` — raise `model.min_vllm_version` from `0.11.0` to `0.17.0` (own commit)
- F-02 `deepseek-ai/DeepSeek-OCR-2.yaml` — raise `model.min_vllm_version` from `0.12.0` to `0.17.0` (own commit)
- F-03 `deepseek-ai/DeepSeek-OCR.yaml` — raise `model.min_vllm_version` from `0.12.0` to `0.17.0` (own commit)
- F-04 `deepseek-ai/DeepSeek-R1.yaml` — raise `model.min_vllm_version` from `0.12.0` to `0.14.0` (own commit)
- F-05 `deepseek-ai/DeepSeek-V3.yaml` — raise `model.min_vllm_version` from `0.12.0` to `0.14.0` (own commit)
- F-06 `deepseek-ai/DeepSeek-V4-Flash.yaml` — raise `variants.fp8.min_vllm_version` from `0.20.0` to `0.22.0` (own commit)
- F-07 `deepseek-ai/DeepSeek-V4-Flash.yaml` — raise `variants.nvfp4.min_vllm_version` from `0.20.0` to `0.22.0` (own commit)
- F-08 `deepseek-ai/DeepSeek-V4-Flash.yaml` — raise `model.min_vllm_version` from `0.20.0` to `0.22.0` (own commit)
- F-09 `internlm/Intern-S1.yaml` — raise `model.min_vllm_version` from `0.10.0` to `0.17.0` (own commit)
- F-10 `jinaai/jina-reranker-m0.yaml` — raise `model.min_vllm_version` from `0.8.0` to `0.8.2` (own commit)
- F-11 `meta-llama/Llama-3.1-8B-Instruct.yaml` — raise `model.min_vllm_version` from `0.6.0` to `0.8.2` (own commit)
- F-12 `microsoft/Phi-4-mini-instruct.yaml` — raise `model.min_vllm_version` from `0.7.0` to `0.17.0` (own commit)
- F-13 `MiniMaxAI/MiniMax-M2.1.yaml` — raise `model.min_vllm_version` from `0.11.0` to `0.15.0` (own commit)
- F-14 `MiniMaxAI/MiniMax-M2.yaml` — raise `model.min_vllm_version` from `0.11.0` to `0.15.0` (own commit)
- F-15 `mistralai/Ministral-3-14B-Instruct-2512.yaml` — raise `model.min_vllm_version` from `0.11.0` to `0.17.0` (own commit)
- F-16 `mistralai/Ministral-3-8B-Reasoning-2512.yaml` — raise `model.min_vllm_version` from `0.11.0` to `0.17.0` (own commit)
- F-17 `mistralai/Mistral-Large-3-675B-Instruct-2512.yaml` — raise `model.min_vllm_version` from `0.11.0` to `0.17.0` (own commit)
- F-18 `nvidia/NVIDIA-Nemotron-Nano-12B-v2-VL-BF16.yaml` — raise `model.min_vllm_version` from `0.11.1` to `0.17.0` (own commit)
- F-19 `openai/gpt-oss-120b.yaml` — raise `model.min_vllm_version` from `0.10.0` to `0.20.0` (own commit)
- F-20 `openai/gpt-oss-20b.yaml` — raise `model.min_vllm_version` from `0.10.0` to `0.13.0` (own commit)
- F-21 `OpenGVLab/InternVL3_5-8B.yaml` — raise `model.min_vllm_version` from `0.10.0` to `0.17.0` (own commit)
- F-22 `PaddlePaddle/PaddleOCR-VL-1.5.yaml` — raise `model.min_vllm_version` from `0.11.1` to `0.17.0` (own commit)
- F-23 `PaddlePaddle/PaddleOCR-VL-1.6.yaml` — raise `model.min_vllm_version` from `0.11.1` to `0.17.0` (own commit)
- F-24 `PaddlePaddle/PaddleOCR-VL.yaml` — raise `model.min_vllm_version` from `0.11.1` to `0.17.0` (own commit)
- F-25 `Qwen/Qwen2.5-VL-72B-Instruct.yaml` — raise `model.min_vllm_version` from `0.7.0` to `0.17.0` (own commit)
- F-26 `Qwen/Qwen2.5-VL-7B-Instruct.yaml` — raise `model.min_vllm_version` from `0.7.0` to `0.17.0` (own commit)
- F-27 `Qwen/Qwen3-VL-235B-A22B-Instruct.yaml` — raise `model.min_vllm_version` from `0.11.0` to `0.17.0` (own commit)
- F-28 `Qwen/Qwen3-VL-30B-A3B-Instruct.yaml` — raise `model.min_vllm_version` from `0.11.0` to `0.17.0` (own commit)
- F-29 `Qwen/Qwen3.5-122B-A10B.yaml` — raise `model.min_vllm_version` from `0.17.0` to `0.20.0` (own commit)
- F-30 `Qwen/Qwen3.5-397B-A17B.yaml` — raise `model.min_vllm_version` from `0.17.0` to `0.20.0` (own commit)
- F-31 `tencent/HunyuanOCR.yaml` — raise `model.min_vllm_version` from `0.11.0` to `0.17.0` (own commit)
- F-32 `zai-org/GLM-4.5V.yaml` — raise `model.min_vllm_version` from `0.12.0` to `0.17.0` (own commit)
- F-33 `zai-org/GLM-4.6V.yaml` — raise `model.min_vllm_version` from `0.12.0` to `0.17.0` (own commit)
- F-34 `zai-org/GLM-OCR.yaml` — raise `model.min_vllm_version` from `0.12.0` to `0.17.0` (own commit)
- F-35 `zai-org/Glyph.yaml` — raise `model.min_vllm_version` from `0.11.0` to `0.17.0` (own commit)

## Needs human judgment (87)
- F-36 `baidu/ERNIE-4.5-VL-28B-A3B-PT.yaml` `--disable-log-requests` — --disable-log-requests is gone at v0.29.0, but upstream documents no replacement — deleting it and re-spelling it are different edits
- F-37 `bosonai/higgs-audio-v3-tts-4b.yaml` `--omni` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-38 `deepseek-ai/DeepSeek-R1.yaml` `VLLM_ENGINE_READY_TIMEOUT_S` — the guide shows flags newer than the pinned floor 0.12.0; the guide text is what needs updating, not necessarily the pin
- F-39 `deepseek-ai/DeepSeek-V3.yaml` `VLLM_ENGINE_READY_TIMEOUT_S` — the guide shows flags newer than the pinned floor 0.12.0; the guide text is what needs updating, not necessarily the pin
- F-40 `deepseek-ai/DeepSeek-V4-Flash.yaml` `VLLM_MEMORY_PROFILE_INCLUDE_ATTN` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-41 `deepseek-ai/DeepSeek-V4-Flash.yaml` `VLLM_USE_B12X_WO_PROJECTION` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-42 `deepseek-ai/DeepSeek-V4-Flash.yaml` `VLLM_USE_B12X_MHC` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-43 `deepseek-ai/DeepSeek-V4-Flash.yaml` `VLLM_USE_B12X_FP8_GEMM` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-44 `deepseek-ai/DeepSeek-V4-Flash.yaml` `VLLM_USE_B12X_MOE` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-45 `deepseek-ai/DeepSeek-V4-Flash.yaml` `VLLM_USE_B12X_SPARSE_INDEXER` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-46 `deepseek-ai/DeepSeek-V4-Pro.yaml` `--prefill-schedule-interval` — the guide shows flags newer than the pinned floor 0.20.0; the guide text is what needs updating, not necessarily the pin
- F-47 `fishaudio/s2-pro.yaml` `--omni` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-48 `inclusionAI/Ming-omni-tts-0.5B.yaml` `--omni` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-49 `jinaai/jina-reranker-m0.yaml` `VLLM_ROCM_USE_AITER` — the guide shows flags newer than the pinned floor 0.8.0; the guide text is what needs updating, not necessarily the pin
- F-50 `Lightricks/LTX-2.5-Diffusers.yaml` `--diffusion-attention-backend` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-51 `Lightricks/LTX-2.5-Diffusers.yaml` `--model-class-name` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-52 `meta-llama/Llama-3.1-8B-Instruct.yaml` `VLLM_ROCM_USE_AITER` — the guide shows flags newer than the pinned floor 0.6.0; the guide text is what needs updating, not necessarily the pin
- F-53 `meta-llama/Llama-3.1-8B-Instruct.yaml` `--disable-log-requests` — --disable-log-requests is gone at v0.29.0, but upstream documents no replacement — deleting it and re-spelling it are different edits
- F-54 `meta-llama/Llama-3.3-70B-Instruct.yaml` `--disable-log-requests` — --disable-log-requests is gone at v0.29.0, but upstream documents no replacement — deleting it and re-spelling it are different edits
- F-55 `meta-llama/Llama-4-Scout-17B-16E-Instruct.yaml` `--disable-log-requests` — --disable-log-requests is gone at v0.29.0, but upstream documents no replacement — deleting it and re-spelling it are different edits
- F-56 `MiniMaxAI/MiniMax-H3.yaml` `--num-gpus` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-57 `MiniMaxAI/MiniMax-H3.yaml` `--usp` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-58 `MiniMaxAI/MiniMax-H3.yaml` `--ring` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-59 `MiniMaxAI/MiniMax-H3.yaml` `--vae-patch-parallel-size` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-60 `MiniMaxAI/MiniMax-H3.yaml` `--vae-parallel-mode` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-61 `MiniMaxAI/MiniMax-H3.yaml` `--vae-use-tiling` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-62 `MiniMaxAI/MiniMax-H3.yaml` `VLLM_OMNI_VIDEO_SYNC_TIMEOUT` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-63 `MiniMaxAI/MiniMax-H3.yaml` `--task` — --task is gone at v0.29.0, but upstream documents no replacement — deleting it and re-spelling it are different edits
- F-64 `MiniMaxAI/MiniMax-H3.yaml` `--task-type` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-65 `MiniMaxAI/MiniMax-H3.yaml` `--init-timeout` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-66 `MiniMaxAI/MiniMax-H3.yaml` `--diffusion-attention-backend` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-67 `MiniMaxAI/MiniMax-H3.yaml` `--text-encoder-tp-size` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-68 `MiniMaxAI/MiniMax-H3.yaml` `--stage-init-timeout` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-69 `MiniMaxAI/MiniMax-H3.yaml` `--enable-distributed-layerwise-offload` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-70 `MiniMaxAI/MiniMax-H3.yaml` `--dlo-no-use-allgather` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-71 `MiniMaxAI/MiniMax-H3.yaml` `--dlo-resident-layers` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-72 `mistralai/Voxtral-4B-TTS-2603.yaml` `--omni` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- F-73 `moonshotai/Kimi-K2-Instruct.yaml` `--disable-log-requests` — --disable-log-requests is gone at v0.29.0, but upstream documents no replacement — deleting it and re-spelling it are different edits
- F-74 `moonshotai/Kimi-K3.yaml` `--dcp-comm-backend` — a changed upstream default is a behaviour question for the recipe author
- F-75 `moonshotai/Kimi-K3.yaml` `VLLM_ROCM_USE_AITER_MOE_SITUV2` — not shipped by vLLM at any indexed tag; most likely a plugin flag this tool cannot see
- …and 47 more in report.md

## Skipped (0)
_none_
